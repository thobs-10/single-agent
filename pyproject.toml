[build-system]
requires = ["uv_build>=0.8.19,<0.9.0"]
build-backend = "uv_build"

[project]
name = "single-agent"
version = "0.1.0"
description = "Local-first Gmail newsletter summarizer agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
	"loguru>=0.7.3",
	"pydantic>=2.8.0",
	"pydantic-settings>=2.4.0",
	"python-dotenv>=1.0.1",
]

[project.scripts]
single-agent = "main:main"

[tool.uv.build-backend]
module-name = "single_agent"

[tool.setuptools_scm]
write_to = "_version.py"
version_scheme = "release-branch-semver"

[dependency-groups]
dev = [
	"mypy>=1.11.0",
	"pre-commit>=4.6.1",
	"pytest>=8.3.0",
	"pytest-cov>=5.0.0",
	"ruff>=0.5.7",
]
